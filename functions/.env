# Cloud Functions params — empty until Authorize.net credentials are ready.
# When ready, set via Secret Manager or update these values and redeploy.
AUTHORIZE_API_LOGIN_ID=
AUTHORIZE_CLIENT_KEY=
AUTHORIZE_TRANSACTION_KEY=
AUTHORIZE_SANDBOX=true
